{
  "name": "orangehrm-ui-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright UI tests for OrangeHRM demo (QA assessment)",
  "scripts": {
    "test": "playwright test --project=chromium",
    "test:chromium": "playwright test --project=chromium",
    "test:chrome": "playwright test --project=chromium --headed",
    "test:firefox": "playwright test --project=firefox",
    "test:login": "playwright test tests/auth/login.spec.ts --project=chromium --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1",
    "@types/node": "^22.10.2",
    "dotenv": "^16.4.7",
    "typescript": "^5.7.2"
  }
}
